$$
\forall \vec p \forall \vec x\exist y(\phi(\vec x,y,\vec{p})\land\forall z(\phi(\vec x,z,\vec{p})\rightarrow z=y)) \rightarrow

\forall \vec p \forall \vec x\forall y(f(\vec{x})=y\leftrightarrow \phi(\vec x,y,\vec{p}))
$$


$$
\forall E(
\forall \vec p \forall \vec x(\vec{x} \in E \rightarrow \exist y (\phi(\vec x,y,\vec{p})\land\forall z(\phi(\vec x,z,\vec{p})\rightarrow z=y)) \rightarrow

\forall \vec p \forall \vec x\forall y(\vec{x} \in E \rightarrow(f(\vec{x})=y\leftrightarrow \phi(\vec x,y,\vec{p}))))
$$